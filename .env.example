# Public browser values from Supabase Dashboard → Connect. These are safe to expose.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Do not add Stripe secrets, the Supabase service-role key, or webhook signing secrets here.
# Set those only with `supabase secrets set` for the Edge Functions.
